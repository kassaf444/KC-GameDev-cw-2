using System.Collections;
using System.Collections.Generic;
using UnityEngine;
"'name and age "
public class DAY2 : MonoBehaviour 
{ int age= 18 
    // Start is called before the first frame update
    void Start()
    {string name = ''khalid'' 
        print(my  age  is "" + age):
    }bool is champion = true; 
   
"'speed"
    // Update is called once per frame
    void Update()
    {
        float speed = 33.5f; 
    }print(" the friend name is " + name +"and his age is " +age);
    print(" the prophecy says that he is a " + is hero + "hero with aspeed of " + speed);
     "done"